# IELTS G 类真题交接文档（2026-08-15 晚）

> 背景：A 类 17 册已完成入库（剑4-20，剑1-3 放弃）。G 类 14 册（剑4-17）未开始。
> 经估算 G 类约需 18 小时（2-3 个工作日），用户决定先记录交接信息，明天继续。

## 一、A 类现状（已完成 ✅）

- **17 册完整**：剑4-20（除 1-3），每册 320 题 + 320 答案
- **数据库**：IELTS books=17, questions=5413, 剑17 音频 16 个
- **解析脚本**（`src/scripts/zhenti/`）：
  - `parse_ielts_all.py`：标准版（剑4-9、11-19），自动探测 + 视觉兜底 + 双 provider
  - `parse_ielts20.py`：剑20 分册版（答案内嵌题目页）
  - `parse_ielts10.py`：剑10 版（紧凑答案表）
  - 缓存：`cache/ielts_q_*.json` + `ielts_ans_*.json`（断点续跑）

## 二、G 类数据源

路径：`E:\work\data\English\IELTS\剑桥雅思真题4-19-A+G类\雅思G类真题4-17\`

| 文本层 | 册数 | 剑册 |
|---|---|---|
| ✅ 有文本层 | 3 | 剑4、10、13 |
| ⚠️ 扫描件 | 11 | 剑5-9、11、12、14-17 |

## 三、G 类结构（与 A 类差异，关键！）

- **听力**：与 A 类相同（4 sections × 10 题 = 40 题）
- **阅读**：3 个 **Section**（非 Passage），题量分布不同：
  - Section 1：2-3 篇短文，**13 题**
  - Section 2：2 篇工作场景短文，**13 题**
  - Section 3：1 篇长文，**14 题**
- 每 Test 仍 80 题（40 听力 + 40 阅读）
- **答案页**：每册位置需探测（G 类答案页可能紧凑表或分 Test 页）

## 四、G 类推进方案

### 复用 A 类基础设施
1. **解析器**：`parse_ielts_all.py` 基本可用，但需适配：
   - 阅读题量分布：Section 1=1-13、Section 2=14-26、Section 3=27-40（与 A 类 R1-13/R2-14-26/R3-27-40 相同！只是叫 Section 不叫 Passage）
   - 探测正则：`SECTION N` 已支持（A 类老册剑4-10 也是 SECTION 格式）
   - **实际上 G 类阅读可以直接复用 A 类的 read 解析逻辑**（题号 1-13/14-26/27-40 分布一致）
2. **扫描件视觉探测**：剑19/16 已验证的 `_scan_vis.py` 流程（逐页分类 + 手动 probe_final）
3. **双 provider**：agnes 主力 + stepfun 兜底 429，已固化在 parse_ielts_all.py

### 预计工作量
| 册类型 | 册数 | 每册耗时 | 小计 |
|---|---|---|---|
| 有文本层（剑4/10/13） | 3 | 0.5h | 1.5h |
| 扫描件（剑5-9/11/12/14-17） | 11 | 1.5h | 16.5h |
| **合计** | **14** | | **~18h（2-3 工作日）** |

API 调用估算：约 866 次（文本层 96 + 扫描件 770），agnese 为主力消耗可控。

## 五、明天开工步骤（Checklist）

1. `git` 确认 `src/scripts/zhenti/` 状态（脚本已就位）
2. 先做**剑13 G**（有文本层，可自动探测）验证 G 类流程
3. 再做**剑10 G、剑4 G**（文本层）
4. 然后扫描件册：逐册 `_scan_vis.py` 视觉探测 → 建 probe_final → `parse_ielts_all.py`
5. 入库：`import-ielts.ts`（需确认 G 类 source 命名 `ielts:G:...` 不与 A 类冲突）

## 六、已知坑（务必先看）

1. **G 类阅读叫 Section 不叫 Passage**——解析输出用 passage 字段但题号分布同 A 类
2. **答案页紧凑表**（剑10 G 可能同剑10 A）：只 2 页覆盖 4 Test，视觉识别时 R 部分易漏，需单独补
3. **扫描件册**：必须视觉探测页面（无文本层），参考剑19/16 流程
4. **缓存键隔离**：`ielts_q_B{book}_...`，G 类建议用 `ieltsG_q_B{book}_...` 前缀避免与 A 类冲突
5. **入库 sourceUrl**：A 类用 `ielts:17:test:1:...`，G 类建议 `ielts:G:17:test:1:...` 区分
6. **音频**：G 类音频未排查（`雅思G类音频4-17` 目录是否存在需确认）
