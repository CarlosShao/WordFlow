# Desktop

Tauri desktop application shell.

## Structure

- `src/` - Frontend entry for desktop
- `src-tauri/` - Tauri / Rust backend (generated later)

## Scripts

- `npm run tauri dev` - start desktop dev
- `npm run tauri build` - build desktop app
